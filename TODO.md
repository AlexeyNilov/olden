# Backlog

## Entry template

```markdown
* [ ] task description
```

## Actual tasks

* [ ] Add Milestone 6 glossary terms for the local unit data catalog.
* [ ] Add Milestone 6 requirements for loading, validating, and querying unit catalog records.
* [ ] Record an ADR for a YAML-backed, locally packaged unit catalog with CC BY-SA data separated from CC0 code.
* [ ] Add the YAML parser dependency and package-data configuration for bundled catalog files.
* [ ] Create `olden.unit_data` modules for typed catalog records, strict YAML loading, duplicate-ID rejection, missing-ID errors, and `UnitDefinition` conversion.
* [ ] Add a CC BY-SA license/notice boundary for bundled unit catalog data.
* [ ] Add an initial attributed sample unit catalog entry using quoted date strings and source metadata.
* [ ] Write failing tests before implementation for catalog loading, lookup, duplicate IDs, missing IDs, validation, and `UnitDefinition` conversion.
* [ ] Bump the package version for the new public data-layer behavior.
* [ ] Run `make test`, `make format`, `make lint`, and `make mypy`.
